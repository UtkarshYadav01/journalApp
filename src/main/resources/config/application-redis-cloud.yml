spring:
  redis:
    host: ${REDIS_HOST}
    port: 16561
    password: ${REDIS_PASSWORD}
#      database: 0
#      timeout: 0
#      pool:
#        max-active: 8
#        max-idle: 8
#        min-idle: 0
#        max-wait: -1